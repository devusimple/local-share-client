# Set this on your Vercel dashboard (Settings → Environment Variables)
# to connect the client to your server deployment.
# Leave unset for local development (Vite proxy handles it).
VITE_SERVER_URL=https://local-share-server.vercel.app
